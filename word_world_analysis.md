
# word_world 프로젝트 분석 보고서

## 1. 프로젝트 개요

`word_world`는 React Native로 개발된 모의고사 기반의 영어 단어, 문장 해석, 주제 암기 학습 애플리케이션입니다. 사용자는 연도, 월, 학년별로 시행된 모의고사를 선택하고, 해당 시험에 출제된 어휘, 문장, 주제를 효율적으로 학습할 수 있습니다.

주요 기능은 다음과 같습니다.
- **모의고사 선택:** 학년, 연도별로 필터링하여 원하는 모의고사를 선택합니다.
- **학습 유형:** 선택한 모의고사에 대해 '어휘', '해석', '주제' 세 가지 유형의 학습을 제공합니다.
- **효율적인 암기:** '알고 있음' / '모르겠음'을 체크하여 모르는 내용만 반복 학습(회독)하는 기능을 지원합니다.
- **학습 시간 측정:** 각 학습 세션의 소요 시간을 측정하여 보여줍니다.
- **동적 데이터 관리:** `wordFileMap.js`, `interpretationFileMap.js`, `topicFileMap.js`를 통해 모의고사별 학습 데이터를 유연하게 추가하고 관리할 수 있습니다.

---

## 2. 프로젝트 구조 및 파일 설명

프로젝트는 React Navigation을 사용한 화면(Screen) 기반 아키텍처를 따릅니다. 데이터는 `data` 폴더 내의 JSON 파일들로 관리되며, 각 화면은 특정 기능을 담당합니다.

### 주요 파일 및 역할

- **`App.js`**: 앱의 최상위 컴포넌트. React Navigation의 `StackNavigator`를 설정하여 전체 화면 전환 로직을 관리합니다.
- **`csv-to-json.js`**: `word_template.csv` 파일을 읽어 `word.json` 형식으로 변환하는 유틸리티 스크립트입니다. 단어 데이터를 쉽게 추가할 수 있도록 돕습니다.

- **`screens/`**: 각 화면을 구성하는 컴포넌트들이 위치합니다.
    - **`HomeScreen.js`**: 앱의 메인 화면. 학년, 연도를 필터링하여 학습할 모의고사를 선택하는 UI를 제공합니다.
    - **`MockTestDetailScreen.js`**: 선택한 모의고사에 대해 '어휘', '해석', '주제' 학습으로 진입하는 메뉴를 보여줍니다.
    - **`VocabularySelectRangeScreen.js`**: 어휘 학습 범위를 선택하는 화면입니다. (예: 1-50번, 1-100번)
    - **`VocabularyScreen.js`**: 단어 암기 화면. 단어 카드를 넘기며 '알고 있음'/'모르겠음'을 체크하고, 모르는 단어만 반복 학습할 수 있습니다.
    - **`InterpretationSelectNumberScreen.js`**: 해석 학습을 진행할 문제 번호를 선택하는 화면입니다.
    - **`InterpretationScreen.js`**: 문장 해석 학습 화면. 영어 문장을 보고 한국어 뜻을 맞추는 방식으로 진행됩니다.
    - **`TopicScreen.js`**: 지문의 주제를 암기하는 화면. 지문을 보고 영어/한국어 주제를 확인하며 암기합니다.

- **`data/`**: 학습 데이터를 관리합니다.
    - **`wordFileMap.js`, `interpretationFileMap.js`, `topicFileMap.js`**: `연도_월_학년` 형식의 키(Key)와 해당 데이터 파일(JSON)을 매핑하는 파일입니다. 이 파일을 수정하여 새로운 모의고사 데이터를 쉽게 추가할 수 있습니다.
    - **`word/`, `interpretation/`, `topic/`**: 실제 학습 데이터인 JSON 파일들이 연도별로 저장되어 있습니다.

---

## 3. 개선 및 추가 기능 제안

### 3.1. 코드 및 구조 개선

1.  **컴포넌트 분리 및 재사용**:
    - `HomeScreen.js`나 `TopicScreen.js` 등 여러 화면에서 사용되는 버튼, 카드 UI(`TouchableOpacity`)를 별도의 재사용 가능한 컴포넌트(예: `CustomButton.js`, `Card.js`)로 분리하면 코드 중복을 줄이고 유지보수성을 높일 수 있습니다.
    - 각 화면 내부에 정의된 `HomeContainer`, `TopicContainer` 같은 컨테이너 컴포넌트들을 `components` 폴더를 만들어 통합 관리하는 것을 추천합니다.

2.  **상태 관리 라이브러리 도입**:
    - 현재는 `useState`와 `route.params`를 통해 상태를 관리하고 있지만, 앱 규모가 커지면 데이터 흐름 추적이 어려워집니다. `Redux Toolkit`이나 `Zustand` 같은 상태 관리 라이브러리를 도입하여 '아는 단어/모르는 단어' 목록, 사용자 설정 등을 전역적으로 관리하면 코드가 훨씬 깔끔해집니다.

3.  **데이터 로딩 로직 개선**:
    - `useEffect` 안에서 `require`를 동적으로 사용하고 있는데, 데이터가 많아지면 초기 로딩 성능에 영향을 줄 수 있습니다. `AsyncStorage`를 활용하여 한번 로드한 데이터를 캐싱하거나, 필요할 때 비동기적으로 파일을 읽어오는 방식으로 개선할 수 있습니다.

4.  **스타일 관리**:
    - `StyleSheet.create`를 화면마다 사용하고 있는데, 공통 스타일(색상, 폰트 크기, 버튼 스타일 등)을 `styles/theme.js` 같은 파일로 분리하여 일관성을 유지하고 관리를 용이하게 할 수 있습니다.

### 3.2. 추가 기능 제안

1.  **오답노트 기능**:
    - '모르겠음'으로 체크한 단어와 문장들을 별도의 '오답노트' 메뉴에서 모아보고, 다시 학습할 수 있는 기능을 추가하면 학습 효과를 극대화할 수 있습니다. `AsyncStorage`에 모르는 항목의 ID를 저장하여 구현할 수 있습니다.

2.  **단어/문장 검색 기능**:
    - 전체 데이터베이스에서 특정 단어나 문장을 검색하여 그 의미와 예문을 바로 확인할 수 있는 기능을 추가하면 유용할 것입니다.

3.  **학습 통계 시각화**:
    - 누적 학습 시간, 회독 수, 아는 단어의 비율 등을 그래프나 차트로 시각화하여 보여주는 '학습 리포트' 화면을 추가하면 사용자의 학습 동기를 부여하는 데 도움이 됩니다.

4.  **사용자 맞춤 설정**:
    - 단어 카드가 넘어가는 애니메이션 효과 선택, 폰트 크기 조절, 다크 모드 지원 등 사용자 경험을 향상시킬 수 있는 설정 기능을 추가하는 것을 고려해볼 수 있습니다.

5.  **푸시 알림**:
    - 정해진 시간에 학습을 독려하는 푸시 알림 기능을 추가하여 사용자의 꾸준한 학습을 유도할 수 있습니다.

---
